# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Pipeline outputs (one folder per Run)
projects/

# One-off style/voice tuning outputs
tuning/

# The Meta AI backend's browser profile: the creator's own logged-in session, never committed
browser-profile/

# User-curated music tracks (keep the folder)
music/*
!music/.gitkeep
