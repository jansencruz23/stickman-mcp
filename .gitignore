# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Pipeline outputs (one folder per Run)
projects/

# One-off style/voice tuning outputs
tuning/

# The Meta AI backend's browser profiles: the creator's own logged-in sessions, never committed
browser-profile/
browser-profile-2/

# User-curated music tracks (keep the folder)
music/*
!music/.gitkeep

# Channel two's Runs, same as projects/
projects-stikkky/

# The anime channel's Runs, same again
projects-anime/

# Browser MCP console logs
.playwright-mcp/

# Per Run handoff files channel_run.py reads and rewrites, one Run at a time
scripts/run-id*.txt
scripts/script.json
scripts/metadata.json
